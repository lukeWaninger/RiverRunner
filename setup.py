# This project was not intended to be setup by its proposed user base.
# Furthermore, there are a few server level dependencies (ie. PostgreSQL)
# that would require setup beyond a single Python based setup file.
# Additionally, we do not have the scraped dataset publicly available. The
# static data took some fine-tuning through the RDMS console which is also
# unattainable through Python.
